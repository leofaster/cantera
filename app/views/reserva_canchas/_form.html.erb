<%= form_for(@reserva_cancha) do |f| %>
  <% if @reserva_cancha.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reserva_cancha.errors.count, "error") %> prohibited this reserva_cancha from being saved:</h2>

      <ul>
      <% @reserva_cancha.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :reserva_id %><br />
    <%= f.number_field :reserva_id %>
  </div>
  <div class="field">
    <%= f.label :cancha_id %><br />
    <%= f.number_field :cancha_id %>
  </div>
  <div class="field">
    <%= f.label :monto %><br />
    <%= f.text_field :monto %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
